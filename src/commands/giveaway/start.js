const {SlashCommandBuilder,PermissionFlagsBits}=require('discord.js');
const G=require('../../systems/giveaways');
module.exports={data:new SlashCommandBuilder().setName('giveaway-start').setDescription('بدء قيف أوي').addStringOption(o=>o.setName('duration').setDescription('مثال 10m أو 2h أو 1d').setRequired(true)).addIntegerOption(o=>o.setName('winners').setDescription('عدد الفائزين').setMinValue(1).setMaxValue(20).setRequired(true)).addStringOption(o=>o.setName('prize').setDescription('الجائزة').setMaxLength(200).setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(i){const ms=G.durationMs(i.options.getString('duration'));if(!ms||ms<10000)return i.reply({content:'❌ المدة غير صحيحة. مثال: 10m أو 2h أو 1d.',ephemeral:true});const g={id:Date.now().toString(36),guildId:i.guild.id,channelId:i.channel.id,prize:i.options.getString('prize'),winners:i.options.getInteger('winners'),endsAt:Date.now()+ms,participants:[],ended:false};const m=await i.reply({embeds:[G.embed(g)],components:[G.row(g.id)],fetchReply:true});g.messageId=m.id;const all=G.load();all[g.id]=g;G.save(all);setTimeout(()=>G.end(g.id,i.client),ms);return;}};
