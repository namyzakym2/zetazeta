const {SlashCommandBuilder,PermissionFlagsBits}=require('discord.js'); const G=require('../../systems/giveaways');
module.exports={data:new SlashCommandBuilder().setName('giveaway-reroll').setDescription('اختيار فائز جديد').addStringOption(o=>o.setName('id').setDescription('معرف القيف أوي').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(i){const all=G.load(),g=all[i.options.getString('id')];if(!g||!g.ended)return i.reply({content:'❌ القيف أوي غير موجود أو لم ينتهِ.',ephemeral:true});const pool=[...new Set(g.participants||[])];if(!pool.length)return i.reply({content:'❌ لا يوجد مشاركون.',ephemeral:true});const winner=pool[Math.floor(Math.random()*pool.length)];return i.reply(`🎉 الفائز الجديد: <@${winner}> — **${g.prize}**`);}};
