const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('unbanall').setDescription('إلغاء حظر جميع المحظورين').setDefaultMemberPermissions(PermissionFlagsBits.BanMembers),async execute(i){const bans=await i.guild.bans.fetch();if(!bans.size)return i.reply('ℹ️ لا يوجد محظورون.');let n=0;for(const b of bans.values()){await i.guild.bans.remove(b.user.id,'Unban all').catch(()=>{});n++;}return i.reply(`✅ تم فك حظر ${n} عضو.`);}};