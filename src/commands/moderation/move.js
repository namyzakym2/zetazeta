const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('move').setDescription('نقل عضو صوتياً').addUserOption(o=>o.setName('member').setDescription('العضو').setRequired(true)).addChannelOption(o=>o.setName('channel').setDescription('الروم الصوتي').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.MoveMembers),async execute(i){const m=await i.guild.members.fetch(i.options.getUser('member').id);const c=i.options.getChannel('channel');if(!c.isVoiceBased())return i.reply({content:'❌ اختر رومًا صوتيًا.',ephemeral:true});await m.voice.setChannel(c);return i.reply(`🚚 تم نقل ${m.user.tag} إلى ${c}.`);}};