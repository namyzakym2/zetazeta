const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('roledelete').setDescription('حذف رتبة').addRoleOption(o=>o.setName('role').setDescription('الرتبة').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(i){const r=i.options.getRole('role');await r.delete(`Deleted by ${i.user.tag}`);return i.reply(`🗑️ تم حذف الرتبة **${r.name}**.`);}};