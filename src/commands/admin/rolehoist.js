const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('rolehoist').setDescription('تفعيل أو تعطيل ظهور الرتبة').addRoleOption(o=>o.setName('role').setDescription('الرتبة').setRequired(true)).addBooleanOption(o=>o.setName('enabled').setDescription('تفعيل').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(i){const r=i.options.getRole('role');await r.setHoist(i.options.getBoolean('enabled'));return i.reply('✅ تم تحديث الرتبة.');}};