const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('nickall').setDescription('تعيين لقب للجميع').addStringOption(o=>o.setName('nickname').setDescription('اللقب').setMaxLength(32).setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageNicknames),async execute(i){await i.guild.members.fetch();let n=0;for(const m of i.guild.members.cache.values()){if(m.user.bot)continue;await m.setNickname(i.options.getString('nickname')).catch(()=>{});n++;}return i.reply(`🏷️ تمت محاولة تغيير لقب ${n} عضو.`);}};