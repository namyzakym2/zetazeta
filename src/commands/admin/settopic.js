const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('settopic').setDescription('تغيير موضوع الروم').addStringOption(o=>o.setName('topic').setDescription('الموضوع').setMaxLength(1024).setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageChannels),async execute(i){if(!i.channel.isTextBased()||!i.channel.setTopic)return i.reply({content:'❌ هذا الروم لا يدعم الموضوع.',ephemeral:true});await i.channel.setTopic(i.options.getString('topic'));return i.reply('✅ تم تغيير موضوع الروم.');}};