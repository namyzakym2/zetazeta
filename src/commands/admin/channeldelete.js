const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('channeldelete').setDescription('حذف الروم الحالي').setDefaultMemberPermissions(PermissionFlagsBits.ManageChannels),async execute(i){const name=i.channel.name;await i.channel.delete(`Deleted by ${i.user.tag}`);return i.reply(`🗑️ تم حذف #${name}.`).catch(()=>{});}};