const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('purge').setDescription('حذف رسائل').addIntegerOption(o=>o.setName('amount').setDescription('العدد').setMinValue(1).setMaxValue(100).setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageMessages),async execute(i){const a=i.options.getInteger('amount');const deleted=await i.channel.bulkDelete(a,true);return i.reply({content:`🧹 تم حذف ${deleted.size} رسالة.`,ephemeral:true});}};