const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('rolemention').setDescription('تفعيل أو تعطيل منشن الرتبة').addRoleOption(o=>o.setName('role').setDescription('الرتبة').setRequired(true)).addBooleanOption(o=>o.setName('enabled').setDescription('تفعيل').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(i){const r=i.options.getRole('role');await r.setMentionable(i.options.getBoolean('enabled'));return i.reply('✅ تم تحديث قابلية المنشن.');}};