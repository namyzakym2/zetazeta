const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('rolecolor').setDescription('تغيير لون رتبة').addRoleOption(o=>o.setName('role').setDescription('الرتبة').setRequired(true)).addStringOption(o=>o.setName('hex').setDescription('مثال #8b5cf6').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(i){const hex=i.options.getString('hex');if(!/^#[0-9a-fA-F]{6}$/.test(hex))return i.reply({content:'❌ اللون يجب أن يكون مثل #8b5cf6',ephemeral:true});const r=i.options.getRole('role');await r.setColor(hex);return i.reply(`🎨 تم تغيير لون ${r}.`);}};