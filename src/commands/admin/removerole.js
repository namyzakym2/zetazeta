const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('removerole').setDescription('إزالة رتبة من عضو').addUserOption(o=>o.setName('member').setDescription('العضو').setRequired(true)).addRoleOption(o=>o.setName('role').setDescription('الرتبة').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles),async execute(i){const m=await i.guild.members.fetch(i.options.getUser('member').id);const r=i.options.getRole('role');await m.roles.remove(r);return i.reply(`➖ تمت إزالة ${r} من ${m.user.tag}.`);}};