const { SlashCommandBuilder }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('choose').setDescription('اختيار بين خيارات').addStringOption(o=>o.setName('options').setDescription('افصل الخيارات بفاصلة').setRequired(true)),async execute(i){const a=i.options.getString('options').split(',').map(x=>x.trim()).filter(Boolean);if(a.length<2)return i.reply({content:'❌ اكتب خيارين على الأقل مفصولين بفاصلة.',ephemeral:true});return i.reply(`🎯 الاختيار: **${a[Math.floor(Math.random()*a.length)]}**`);}};