const { SlashCommandBuilder, PermissionFlagsBits }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('invite').setDescription('إنشاء دعوة').addIntegerOption(o=>o.setName('minutes').setDescription('مدة الدعوة بالدقائق').setMinValue(0).setMaxValue(10080)).setDefaultMemberPermissions(PermissionFlagsBits.CreateInstantInvite),async execute(i){if(!i.channel.createInvite)return i.reply({content:'❌ لا يمكن إنشاء دعوة هنا.',ephemeral:true});const m=i.options.getInteger('minutes')||0;const inv=await i.channel.createInvite({maxAge:m*60,maxUses:0,reason:`By ${i.user.tag}`});return i.reply(`🔗 ${inv.url}`);}};