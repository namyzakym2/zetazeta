const { SlashCommandBuilder, EmbedBuilder }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('permissions').setDescription('عرض صلاحيات عضو').addUserOption(o=>o.setName('member').setDescription('العضو')),async execute(i){const m=i.options.getMember('member')||i.member;const p=m.permissions.toArray();return i.reply({embeds:[new EmbedBuilder().setTitle(`🛡️ صلاحيات ${m.user.tag}`).setDescription(p.map(x=>`• ${x}`).join('\n').slice(0,4000))]});}};