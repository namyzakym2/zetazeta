const { SlashCommandBuilder, EmbedBuilder }=require('discord.js'); const { getZetaBotEmoji }=require('../../utils/zetaEmojis'); module.exports={data:new SlashCommandBuilder().setName('channelinfo').setDescription('معلومات الروم').addChannelOption(o=>o.setName('channel').setDescription('الروم')),async execute(i){const c=i.options.getChannel('channel')||i.channel;return i.reply({embeds:[new EmbedBuilder().setTitle(`${getZetaBotEmoji('channels')||'📁'} ${c.name}`).addFields({name:'ID',value:c.id},{name:'النوع',value:String(c.type)},{name:'الموقع',value:String(c.position)},{name:'الفئة',value:c.parent?.name||'بدون'})]});}};