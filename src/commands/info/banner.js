const { SlashCommandBuilder, EmbedBuilder }=require('discord.js'); module.exports={data:new SlashCommandBuilder().setName('banner').setDescription('عرض بانر العضو').addUserOption(o=>o.setName('member').setDescription('العضو')),async execute(i){const u=await (i.options.getUser('member')||i.user).fetch();const b=u.bannerURL({size:1024});if(!b)return i.reply({content:'❌ لا يوجد بانر لهذا العضو.',ephemeral:true});return i.reply({embeds:[new EmbedBuilder().setTitle(`🖼️ Banner — ${u.tag}`).setImage(b)]});}};